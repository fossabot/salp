# global default image
image: node:latest

cache:
  key: "${CI_COMMIT_REF_SLUG}-${CI_JOB_STAGE}"
  paths:
    - node_modules/

stages:
  - test

# jobs
app:test:unit:
  stage: test
  variables:
    NODE_ENV: "test"
  before_script:
    - npm install
  script:
    - npm run test:unit