# global default image
image: node:latest

cache:
  key: "${CI_COMMIT_REF_SLUG}-${CI_JOB_STAGE}"

stages:
  - test

# jobs
app:test:unit:
  stage: test
  variables:
    NODE_ENV: "test"
  before_script:
    - npm ci
  script:
    - npm run test:unit