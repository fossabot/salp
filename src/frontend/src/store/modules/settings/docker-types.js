// only the configuration mapping types
// use CommonJS so that the application (NodeJS) can access it too
module.exports = {
    SOCKET: 'socket',
    VERIFY_TLS: 'verify_tls',
    CERT_DIR: 'cert_dir',
    BASE_IP: 'base_ip'
}
