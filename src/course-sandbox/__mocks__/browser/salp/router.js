class Router {
    push(to) {
        // do nothing
    }

    handleHostRouteChange(_, to) {
        // do nothing
    }

    _registerHandlers(ipcRenderer) {
        // do nothing
    }
}

export default new Router()
