class Router {
    push(to) {
        // do nothing
    }

    handleHostRouteChange(_, to) {
        // do nothing
    }

    _registerHandlers(ipcRenderer) {
        // do nothing
    }
}

module.exports = new Router()
